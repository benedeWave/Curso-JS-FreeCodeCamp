function convertCtoF(num) {
let converted = (num * (9/5) + 32) 
let convertedNumber = +converted // este paso esta de mas pero asi resolvi xd, ciertamente no se necesita pues estmaos trabajando con num directamente
return convertedNumber // aca cometi errores porque estaba retornando con console.log, pero esto no da mensajes ne el programa sino que es depuracion puramente par aojos del programador.
}
convertCtoF(0)
convertCtoF(-30)
convertCtoF(-10)
convertCtoF(0)
convertCtoF(0)
convertCtoF(20)
convertCtoF(30)